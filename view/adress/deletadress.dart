import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class DeleteAdress extends StatelessWidget {
  const DeleteAdress({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
